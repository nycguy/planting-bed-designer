# Optional. Adds "MapTiler Satellite" as a second imagery source.
# Create a free key at https://cloud.maptiler.com/account/keys/ and restrict it
# to your production origin (Allowed HTTP origins). The free tier has a monthly
# tile-request limit; the app works without this key using Esri World Imagery.
VITE_MAPTILER_KEY=
